# Replace the "ANSWER HERE" with your answer

def remove_elements(list_to_remove_elements):
    list_0 = len(list_to_remove_elements)
    if list_0 < 5:
        return list_to_remove_elements.remove(0)
    elif 5<list_0 and list_0<6:
        return list_to_remove_elements.remove(0)
        return list_to_remove_elements.remove(4)
    else:
        return list_to_remove_elements.remove(0)
        return list_to_remove_elements.remove(4)
        return list_to_remove_elements.remove(5)



def add_elements(list_to_add_elements):
    list_1 = list_to_add_elements
    return list_1.append("Yellow") and list_1.insert(0, "Pink")


def is_empty(list_to_check):
    if len(list_to_check) == 0:
        return True 
    else:
        Return False 


def check_lists(list_to_compare1, list_to_compare2):
   if len(list_to_compare1) + len(list_to_compare2) and  list_to_compare1 [2] == list_to_compare2 [2] 
        return True
    else:
        return False

def list_of_lists(list_of_lists_to_modify):
   a=list_of_lists_to_modify
    del a[0][-1]
    del a[1][0]
    del a[1][-1]
    del a[2][0]
    del a[2][1]
    return a
